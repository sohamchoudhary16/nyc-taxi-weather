# NYC Taxi × Weather: Data Engineering Case

## TL;DR

An end-to-end data pipeline joining 2.96M NYC yellow taxi trips (January 2024)
with hourly weather data to answer: **which zones become supply-constrained in
wet weather?** Transit hubs (West Chelsea/Hudson Yards +15.5%, JFK Airport
+8.6%, Penn Station +6.9%) see increased demand in rain, while tourist and
residential zones see drops (Seaport −27.3%, Financial District −15.3%). The
hardest technical problem was not the analysis but the silent timezone bug
between TLC's naive local timestamps and Open-Meteo's UTC data, which would
shift every record by 4–5 hours if joined naively.

## Run it

```bash
conda create -n nyc-taxi python=3.11 -y && conda activate nyc-taxi
pip install -r requirements.txt
cd dbt && dbt deps && cd ..
make all  # ingest -> load -> dbt build -> tests -> charts   (~5 min cold)
```

No cloud account required.

## Repository structure

```
src/taxi/ Python: ingest, load, timestamps, analysis, fixtures
dbt/models/bronze/ Source definitions (trips, weather, zones)
dbt/models/silver/ stg_trips.sql, stg_weather.sql + schema tests
dbt/models/gold/ fct_trips.sql, agg_zone_hour_demand.sql + schema tests
dbt/macros/ cross_engine.sql (7 macros: DuckDB + Databricks)
dbt/tests/ Singular tests (fan-out guard, future-pickup guard)
tests/ test_timestamps.py (18 cases incl. DST transitions)
docs/ architecture.md, data_quality.md
docs/adr/ 4 architecture decision records (0001-0004)
docs/figures/ 3 analysis charts (auto-generated by analysis.py)
.github/workflows/ CI: ruff + pytest + dbt build with synthetic fixtures
```

## Architecture

See [`docs/architecture.md`](docs/architecture.md). It is structured around four
workloads with different SLAs (receipts, fraud, metrics, taxi placement),
not one monolithic pipeline. The architecture addresses the bidirectional
requirement ("send data back to individual cars") via IoT device shadow,
with command idempotency, TTL, and offline resilience.

Decisions and trade-offs are recorded in [`docs/adr/`](docs/adr/):

| ADR | Decision |
|---|---|
| [0001](docs/adr/0001-duckdb-and-databricks.md) | DuckDB locally, Databricks in cloud |
| [0002](docs/adr/0002-quarantine-not-filter.md) | Quarantine invalid rows, never silent-filter |
| [0003](docs/adr/0003-weather-source.md) | ERA5 reanalysis now, NOAA stations in production |
| [0004](docs/adr/0004-downlink.md) | IoT Core over bare Kafka for the downlink |

## The timezone contract

TLC publishes **naive America/New_York wall-clock time**. Open-Meteo serves
**UTC**. Joining them without an explicit contract silently shifts every record
by 4-5 hours.

The contract is declared in exactly two places:

| Path | Definition |
|---|---|
| `src/taxi/timestamps.py` | Python normalisation with DST handling |
| `dbt/macros/cross_engine.sql` | 7 SQL macros (`local_to_utc`, `utc_to_local`, `trunc_hour`, `date_diff_seconds`, `extract_dow`, `extract_hour`, `bool_or_agg`) |

DST is handled explicitly: spring-forward gaps are quarantined, fall-back
ambiguity is resolved to the first occurrence and flagged. 18 unit tests
cover both transitions.

## Data quality

Of 2,964,624 rows, **924 (0.03%) are quarantined** as invalid. An additional
~10% carry soft flags (null passengers, zero-distance fares, refunds) that are
kept for downstream use. The largest anomaly is a 25.69% fare reconciliation
gap that was traced to a known TLC inconsistency, not a pipeline bug.

Rows are **flagged and quarantined, never silently dropped.** See
[`docs/data_quality.md`](docs/data_quality.md).

## Analysis

**Headline finding:** transit hubs see the strongest demand increase in wet
hours. Passengers who would otherwise walk switch to taxis at stations when it
rains.

| Zone | Demand lift in wet hours |
|---|---|
| West Chelsea/Hudson Yards | +15.5% |
| JFK Airport | +8.6% |
| Penn Station/Madison Sq West | +6.9% |

**Operational recommendation:** reposition vehicles toward transit hubs during
wet hours. Average speed also drops in rain, so each taxi completes fewer
trips per hour. The supply gap is larger than the demand lift alone suggests.

**Caveat:** January only, correlational not causal. A full year would separate
seasonal from weather effects.

Charts are in [`docs/figures/`](docs/figures/):

| Chart | What it shows |
|---|---|
| `demand_by_precipitation.png` | Trip count drops with rain; avg fare spikes in heavy |
| `hourly_heatmap.png` | Hour x day-of-week baseline demand rhythm |
| `zone_wet_lift.png` | Top 20 zones by wet-hour demand lift |

## Data sources

| Source | URL | Format |
|---|---|---|
| NYC TLC Yellow Taxi (Jan 2024) | https://www.nyc.gov/site/tlc/about/tlc-trip-record-data.page | Parquet |
| Open-Meteo ERA5 Reanalysis | https://archive-api.open-meteo.com | JSON -> Parquet |
| TLC Zone Lookup | Bundled with TLC trip data | CSV |

## dbt integration

The pipeline uses dbt-core with the medallion pattern (bronze -> silver -> gold).
All models run against DuckDB locally via `dbt-duckdb`.

```bash
cd dbt
dbt deps                    # install dbt_utils
dbt build --target dev      # build models + run 21 tests
dbt docs generate && dbt docs serve   # browse model docs
```

Source freshness checks are configured on trips and weather tables. Gold models
have schema tests on key columns. Two singular tests guard against join fan-out
and future-dated records.

## Databricks integration

The same dbt models run on Databricks without code changes via 7 cross-engine
macros in `dbt/macros/cross_engine.sql`. To deploy:

1. Copy `dbt/profiles.yml.example` to `dbt/profiles.yml`
2. Set three environment variables:
```bash
   export DATABRICKS_HOST=your-workspace.cloud.databricks.com
   export DATABRICKS_HTTP_PATH=/sql/1.0/warehouses/xxxxx
   export DATABRICKS_TOKEN=dapi_xxxxx
```
3. Upload raw parquet to a Unity Catalog volume
4. Run:
```bash
   cd dbt && dbt build --target databricks
```

See [ADR 0001](docs/adr/0001-duckdb-and-databricks.md) for the rationale.

## Test coverage

| Suite | Count | What it covers |
|---|---|---|
| dbt tests | 21 | Schema (unique, not_null, range) + singular (fan-out, future dates) |
| pytest | 18 | Timestamp normaliser: DST, edge cases, round-trip stability |
| ruff | 0 errors | Linting: pyflakes, pycodestyle, isort, bugbear, simplify |
| CI | All above | GitHub Actions on every push: lint + pytest + fixtures + dbt build |

## What I would do differently at scale

- **Add months** to validate the weather-demand pattern across seasons
- **Replace ERA5 with NOAA station observations** for auditability (see ADR 0003)
- **Implement fraud detection** as a streaming proof-of-concept before ML
- **Cost model the telemetry pipeline** at 190M events/day, sampling rate
  and retention tier is a tens-of-thousands-per-month decision
- **Integration tests** with a fixture dataset covering ingest and load scripts